# Core dependencies for Dark VizMate (Daigram_2.py)
streamlit>=1.28.0
requests>=2.31.0
graphviz>=0.20.1

# Optional: For PDF conversion script (convert_manual_to_pdf.py)
# Uncomment if you need to generate PDF manuals
# reportlab>=4.0.0
